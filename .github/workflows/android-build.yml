name: Android Build from ZIP (Final)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Extract Android project ZIP
        run: |
          unzip anifh.zip
          rm -f anifh.zip

      - name: Copy GPSTrack.java into Android source
        run: |
          mkdir -p app/src/main/java/anifh/app/webview
          cp GPSTrack.java app/src/main/java/anifh/app/webview/GPSTrack.java

      - name: Grant Gradle permission
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/*.apk
